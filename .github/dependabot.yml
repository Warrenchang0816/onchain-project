version: 2
# Version updates are disabled. Only Dependabot security updates are active.
# Security updates are triggered automatically by GitHub vulnerability alerts
# and do not require entries here.
# Re-enable version updates when intentionally upgrading dependencies.
updates: []
