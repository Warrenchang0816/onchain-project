name: Contracts CI

run-name: Contracts CI / ${{ github.event_name }} / ${{ github.ref_name }}

on:
  pull_request:
    paths:
      - "contract-service/**"
      - ".github/workflows/contracts-ci.yml"
  push:
    branches:
      - main
    paths:
      - "contract-service/**"
      - ".github/workflows/contracts-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: contracts-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: contract-service
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: contract-service/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Compile contracts
        run: npm run compile
