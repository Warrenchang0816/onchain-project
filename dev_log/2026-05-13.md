# 2026-05-13 開發紀錄

## 物件生命週期 + 統一上架頁（PR #61）

**目標：** 在「我的物件」列表加入移除（REMOVED）功能與四狀態篩選；將上架刊登流程從 PropertyEditPage 移至獨立的合併頁面（出租/出售 tab 切換）。

### 後端 (Go)

- `go-service/internal/platform/db/schema.go`（已有 EnsureSchema）：透過 `docker exec onchain-postgres psql` 直接對 DB 執行 migration，擴充 `property_setup_status_check` 至 `('DRAFT', 'READY', 'REMOVED', 'ARCHIVED')`
- `go-service/internal/db/model/property_model.go`：新增 `PropertySetupRemoved = "REMOVED"` 與 `PropertySetupArchived = "ARCHIVED"` 常數
- `go-service/internal/db/repository/property_repo.go`：
  - `ListByOwner` 加 `AND setup_status <> 'REMOVED'`
  - 新增 `HasActiveListing`（UNION 查 rental_listing + sale_listing 活躍狀態）
- `go-service/internal/modules/property/service.go`：
  - `Store` interface 加 `HasActiveListing`
  - 新增 sentinel errors：`ErrPropertyListed`（409）、`ErrInvalidStatus`（422）
  - 新增 `RemoveProperty` 方法（含活躍刊登防衛）
  - `Update` 加 REMOVED/ARCHIVED 防衛（防止覆寫）
  - `AddAttachment` / `UploadPhoto` 的 `computeSetupStatus` 呼叫加 DRAFT/READY 前置判斷（防止覆寫 REMOVED/ARCHIVED）
- `go-service/internal/modules/property/handler.go`：`APIService` interface 加 `RemoveProperty`；新增 handler；`handleErr` 加 409/422 case
- `go-service/internal/bootstrap/router.go`：`protected.PUT("/property/:id/remove", ...)`

### 前端 (TypeScript / React)

- `react-service/src/api/propertyApi.ts`：`PropertySetupStatus` 加 `"ARCHIVED" | "REMOVED"`；新增 `removeProperty`
- `react-service/src/pages/MyPropertiesPage.tsx`（大改寫）：
  - 四個可點擊計數 card（ALL/READY/DRAFT/ARCHIVED），作用中 card 有 highlight
  - property card 狀態 badge（statusBadgeCls / statusLabel）
  - Action 按鈕：DRAFT/READY 顯示「移除物件」；READY 額外顯示「上架」（導 `/listing`）；ARCHIVED 顯示靜態「已歸檔」
  - 移除確認 dialog（含 409 錯誤顯示）
- `react-service/src/pages/PropertyEditPage.tsx`：移除整個「上架刊登」section 及 `isReady`/`useNavigate` 引用
- `react-service/src/components/listing/RentalListingForm.tsx`（新建）：從 RentalListingPage 抽出表單邏輯，接收 `propertyId + property` props
- `react-service/src/pages/RentalListingPage.tsx`：改為薄殼（保留原有路由 `/my/properties/:id/rent`）
- `react-service/src/components/listing/SaleListingForm.tsx`（新建）：從 SaleListingPage 抽出
- `react-service/src/pages/SaleListingPage.tsx`：改為薄殼（保留原有路由 `/my/properties/:id/sale`）
- `react-service/src/pages/PropertyListingPage.tsx`（新建）：出租/出售 tab 切換頁，路由 `/my/properties/:id/listing`；非 READY 物件顯示提示
- `react-service/src/router/index.tsx`：新增 `/my/properties/:id/listing` 路由

### 驗證紀錄

- PASS：`go test ./...`（2/2 property 測試）
- PASS：`npm run lint`（0 errors）
- PASS：`npm run build`（0 errors）

---

## 出租刊登傢俱設備 + 周邊環境欄位補齊（同日）

**目標：** 補齊出租刊登建立/更新表單的傢俱設備（11 項）和周邊環境（4 項）欄位，使填表資料能正確儲存並與公開詳情頁顯示一致。

### 問題根源

DB 和 API response 已有所有欄位，但 Create/Update 輸入路徑全部缺席：`CreateRentalListingRequest`、`UpdateRentalListingRequest`、`repo.Create` SQL、`repo.Update` SQL 均未包含這些欄位。

### 後端 (Go)

- `go-service/internal/modules/rental_listing/dto.go`：`CreateRentalListingRequest` 加 11 傢俱 + 4 環境 `bool`；`UpdateRentalListingRequest` 加同等 `*bool` pointer 欄位
- `go-service/internal/modules/rental_listing/service.go`：`Store.Create` 介面改為接受 `*model.RentalListing`（與 Update 一致）；`service.Create` 建構完整 struct；`applyRentalUpdate` 加 15 個 if 塊；同時修正 `Update/Publish/Close` 將 DB error 與 ErrNotFound 分離
- `go-service/internal/modules/rental_listing/service_test.go`：新增 `TestApplyRentalUpdate_FurnitureNearby` + `TestApplyRentalUpdate_NilFieldsAreNoOp`
- `go-service/internal/db/repository/rental_listing_repo.go`：`Create` 改簽名 + INSERT SQL 補 15 欄（$11–$25）；`Update` SQL SET 子句補 15 欄（$10–$24）

### 前端 (TypeScript / React)

- `react-service/src/api/rentalListingApi.ts`：`CreateRentalListingPayload` 補 15 個 required bool
- `react-service/src/components/listing/RentalListingForm.tsx`：`FormState`/`EMPTY_FORM`/`listingToForm`/`formToPayload` 補 15 欄；新增「傢俱設備」（11 checkbox）+「周邊環境」（4 checkbox）UI section

### 驗證紀錄

- PASS：`go test ./...`（rental_listing 2 新增測試 + 原有 property 測試）
- PASS：`npm run lint`（0 errors）
- PASS：`npm run build`（0 errors）
- PASS：docker compose up --build -d（go-service 重建成功）
