# 2026-05-12 開發紀錄

## CI 修復（PR #47）

**目標：** 修正 Frontend CI lint + build 失敗（兩個 ESLint 錯誤 + 一個 TypeScript 錯誤）

### 變更

- `react-service/src/pages/FavoritesPage.tsx`：
  - 移除未使用的 `ListingType` import（觸發 `no-unused-vars`）
  - 將 `setFavLoading(true)` / `setFavError(null)` 從 `useEffect` 本體移到 `handleTabChange` 事件處理器（違反 `react-hooks/set-state-in-effect`）
- `react-service/src/api/credentialApi.ts`：`uploadCredentialFiles` 的 `mainDoc` 參數從 `File` 改為 `File | undefined`，並加 `if (mainDoc) form.append(...)` 防衛
- `react-service/src/components/credential/CredentialApplicationShell.tsx`：兩處 call site 改為 `mainDoc ?? undefined`，消除 `File | null` 不可賦值給 `File` 的 TypeScript 嚴格模式錯誤

---

## OwnerCredentialPage 兩步驟改版（PR #58）

**目標：** 移除現有「目前狀態」區塊及 CredentialApplicationShell 依賴，改為兩步驟流程：Step 1 填寫物件資料 + 聲明即取得 OWNER credential 並建立物件；Step 2 選填附件提升可信度。

### 後端 (Go)

- `go-service/internal/modules/credential/domain.go`：新增常數 `ReviewRouteDeclarations = "DECLARATIONS"`
- `go-service/internal/modules/credential/service.go`：
  - `normalizeReviewRoute`：加入 `case ReviewRouteDeclarations` — 支援大小寫輸入
  - `CreateSubmission`：加入 DECLARATIONS 分支 — `normalizedType == CredentialTypeOwner && route == ReviewRouteDeclarations` 時直接呼叫 `SaveDecision(PASSED, READY)`，跳過 smart/manual review 流程（與 Tenant PROFILE route 同模式）
  - `AnalyzeSubmission`：加入 `ActivationStatus == ACTIVATED` 防衛，阻止已啟用的 submission 被重新送審覆蓋
  - `UploadFiles`：同樣加入 ACTIVATED 防衛
- `go-service/internal/modules/credential/domain_test.go`：新增 `TestNormalizeReviewRouteDeclarations`（測試大寫 + 小寫輸入）

### 前端 (TypeScript / React)

- `react-service/src/api/credentialApi.ts`：`CredentialReviewRoute` 加入 `"DECLARATIONS"` union member
- `react-service/src/components/credential/ownerFieldParsers.ts`（新建）：
  - `REQUIRED_FIELD_KEYS`（10 個）、`DECLARATION_KEYS`（3 個）、`TOTAL_COMPLETION_ITEMS = 13`
  - `computeCompletion(fields, declarations): number` — `Math.round((已填必填 + 已勾聲明) / 13 * 100)`
  - `parsePropertyFields(fields): UpdatePropertyPayload` — 解析樓層（"5F / 24F"）、格局（"3 房 2 廳 2 衛"）、建物類型（中→英 mapping）
- `react-service/src/components/credential/OwnerStep1Form.tsx`（新建）：純展示元件，接收 fields/declarations/callbacks，渲染 header + 進度條 + 11 個欄位 + 3 項聲明 checkbox + 取消/完成按鈕
- `react-service/src/components/credential/OwnerStep2Upload.tsx`（新建）：選填附件上傳，兩個 CredentialDocumentUploader + 送出智能審核/人工審核/稍後再說
- `react-service/src/pages/OwnerCredentialPage.tsx`（大改寫）：
  - 自管 step 狀態（1 | 2）、fields、declarations、submitting、error、showCancelDialog、showPostCompleteDialog
  - localStorage 草稿（key `owner_credential_draft_v1`）：保留草稿 / 刪除草稿 / 完成後清除
  - `doComplete()`：createProperty → updateProperty → createCredentialSubmission(DECLARATIONS) → activateCredentialSubmission → 彈完成 dialog
  - 已有 OWNER credential → 顯示「你已擁有屋主身份」
  - 移除所有 CredentialRolePage / CredentialApplicationShell 依賴

### 驗證紀錄

- PASS：`go test ./internal/modules/credential/... -v`（20/20）
- PASS：`npm run lint`（0 errors）
- PASS：`npm run build`（0 errors，1578 modules transformed）

---

## 物件照片 Gallery（PR #59）

**目標：** 屋主可在 PropertyEditPage 上傳物件照片（最多 10 張）；所有詳情頁頂端顯示大圖 + 縮圖列 gallery；MyPropertiesPage 卡片顯示縮圖。

### 後端 (Go)

- `go-service/internal/modules/property/service.go`：
  - `Service` struct 新增 `storageSvc *storage.Client` 和 `apiBaseURL string`
  - `NewService` 擴為 4 參數
  - 新增 `UploadPhoto`（MinIO 上傳 → proxy URL 存 attachment，count >= 10 回 400）、`DownloadPhoto`
  - 新增 `newPhotoUUID`、`extFromContentType`、`contentTypeFromFilename` helpers
- `go-service/internal/modules/property/handler.go`：
  - `APIService` interface 加入 `UploadPhoto`、`DownloadPhoto`
  - 新增 `UploadPhoto` handler（multipart，max 10MB，path traversal sanitized）和 `ServePhoto` handler（公開代理）
- `go-service/internal/modules/property/service_test.go`（新建）：`TestUploadPhoto_RejectsAtLimit`、`TestUploadPhoto_PassesCountCheckWith9Photos`
- `go-service/internal/bootstrap/wiring.go`：傳入 `minioClient` + `APP_PUBLIC_URL`（預設 `http://localhost:8081`）
- `go-service/internal/bootstrap/router.go`：
  - `POST /property/:id/attachment/photo`（auth required）
  - `GET /property/:id/photos/:filename`（public）
- `go-service/internal/modules/rental_listing/` + `sale_listing/`：`PropertySummaryResponse` 加 `PhotoURLs []string`；`PropertyStore` interface 加 `ListAttachments`；`ListPublic` / `GetByID` 載入 attachments；`toResponse` 填入 `photo_urls`

### 前端 (TypeScript / React)

- `react-service/src/components/property/PropertyPhotoGallery.tsx`（新建）：共用 read-only gallery，空照片顯示 placeholder，有照片顯示 420px hero + 縮圖列（點擊切換）
- `react-service/src/components/property/PropertyPhotoUploader.tsx`（新建）：屋主上傳/刪除元件，wraps gallery，10 張上限前端也防衛
- `react-service/src/api/propertyApi.ts`：新增 `uploadPropertyPhoto`（FormData multipart）
- `react-service/src/pages/PropertyEditPage.tsx`：新增 Section C1（物件照片），Section C 改名 C2（其他附件），移除 PHOTO 選項，預設改 FLOOR_PLAN
- `react-service/src/pages/MyPropertiesPage.tsx`：物件卡片左側新增縮圖（28×20）或 placeholder
- `react-service/src/api/rentalListingApi.ts` + `saleListingApi.ts`：`PropertySummary` 加 `photo_urls?: string[]`
- `react-service/src/pages/RentDetailPage.tsx` + `SaleDetailPage.tsx`：在 back-link 與 Hero 之間插入 `<PropertyPhotoGallery photos={p?.photo_urls ?? []} />`

### 驗證紀錄

- PASS：`go test ./internal/modules/property/...`（2/2）
- PASS：`npm run lint`（0 errors）
- PASS：`npm run build`（0 errors）
