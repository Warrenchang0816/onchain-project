# 2026-04-29 開發紀錄

## Gate 2 — 物件管理主線 (PR #21)

- 後端新增 `property` 完整 CRUD：Create / Edit（含完整度計算）/ 屋主物件列表 / 公開物件詳情 API。
- 後端新增 `rental_listing` 和 `sale_listing` 建立 / 更新 / 發布 / 關閉 API，掛在 `/api/property/:id` 下。
- 前端實作 `PropertyCreatePage`：表單含建物類型、格局、面積、停車、管理費、朝向等欄位。
- 前端實作 `PropertyEditPage`：含完整度進度條、附件上傳（合約、謄本）UI。
- 前端實作 `RentalListingPage`：租賃刊登設定頁（月租、押金、寵物、開伙等條件）。
- 前端實作 `SaleListingPage`：出售刊登設定頁（總價、建坪單價、停車費等）。

## 驗證紀錄

- PASS：Go backend build
- PASS：TypeScript tsc --noEmit
