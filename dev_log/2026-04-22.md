# 2026-04-22 開發日誌

規格：變動原因及邏輯 = 時間 + 變動部分

## 主幹 Gate Roadmap 正式同步到專案文件

- 變動部分：`docs/開發規劃書.md`
- 變動原因及邏輯：將 2026-04-22 定稿的主幹 gate roadmap 正式同步到專案主規劃文件，新增 `2.5 正式主幹 Gate Roadmap` 與 `3.5 Gate-based 開工規則`。從這一天起，後續主幹開發不再只靠對話或單一 spec 記憶，而是以正式文件明確規定 `Gate 0 -> Gate 5` 的順序、支援軌邊界，以及 `spec -> implementation plan -> implementation -> verification` 的開工治理。

- 變動部分：`docs/架構設計書.md`
- 變動原因及邏輯：把中程鏈上化順序與架構治理邊界寫回架構文件，明確指定 `Property -> Agency -> Case -> Stake` 的上鏈順序，並補強 `租屋主線`、`OWNER 自營 + OWNER -> AGENT 授權並存`、鏈上最小必要資料、支援軌不阻塞主幹 Gate 等核心原則，避免架構文件仍停留在只有 phase 名稱、沒有 gate 驗收秩序的狀態。

- 變動部分：`docs/superpowers/specs/2026-04-22-platform-mainline-gate-roadmap-design.md`
- 變動原因及邏輯：此文件維持為主幹 gate roadmap 的上位治理規格；本次不是重寫內容，而是把其已定稿決策同步到專案正式入口文件，讓後續接手者先看主文件即可理解方向，再進一步追到完整規格。

## 後續接手方式調整

- 變動部分：文件使用順序（治理慣例）
- 變動原因及邏輯：後續若要開工主幹功能，應先讀 `docs/開發規劃書.md` 確認當前 Gate 與治理規則，再讀 `docs/架構設計書.md` 對齊分層與中程鏈上化順序，最後回到 `docs/superpowers/specs/2026-04-22-platform-mainline-gate-roadmap-design.md` 查看完整 Gate 定義與驗收欄位。如此可降低文件漂移和 handoff 斷層。

## 本次性質

- 變動部分：文件 / 日誌同步
- 變動原因及邏輯：本次更新屬治理文件同步與 handoff 整理，未新增 API、未修改合約、未調整 DB schema，也未改動前後端執行邏輯。

## 其餘正式文件分層同步

- 變動部分：`docs/可信房屋媒合平台：MVP 階段開發規格與規劃書.md`、`docs/第六階段架構定稿：可信房屋媒合平台.md`、`docs/主權仲介與Owner-first對應影響清單.md`
- 變動原因及邏輯：依「分層同步版」原則保留藍圖與產品定位，不重寫原願景，但補入截至 2026-04-22 的現況對照、Gate 對應與 live file 準據，避免接手者把藍圖文件誤當成目前已全部落地的實作狀態。

- 變動部分：`docs/Docker 標準化部署指南.md`、`docs/區塊鏈與合約建立指南.md`、`docs/區塊鏈專案開發prompt.txt`
- 變動原因及邏輯：這批操作 / 指南文件原本仍大量殘留 `Task Tracker` 專案名稱、`/api/tasks` 路徑與舊合約語境，會直接誤導後續開發與接手，因此改成以目前房屋平台 repo、現行 `.env.example`、`deploy:house-platform:sepolia`、以及 Gate 邊界為準的版本。

- 變動部分：`docs/database/relational-database-spec.md`、`docs/database/relational-database-spec.csv`、`docs/database/relational-database-spec.xlsx`
- 變動原因及邏輯：將 database 規格重新對齊 `infra/init/01-init.sql` 到 `07-listings.sql` 與目前 repo 的 Go module / repository 使用現況，明確區分正式主線表、支援 / indexer 表、與仍存在但不屬主幹的 legacy compatibility tables，避免文件繼續把 `tasks` 誤寫成房屋平台主線或把 `listings` 誤寫成尚未進入 live schema。
## Gate 0 baseline convergence execution

- Added a real `/listings/new` flow on the live listing API and shared the listing editor between create and edit paths.
- Replaced fake listing placeholders on `/`, `/listings`, `/listings/:id`, and `/member` with honest live-state UI.
- Removed all remaining frontend Task Tracker residue and deleted the old `go-service/docs/Onchain Task Tracker*` guides.
- Downgraded `/logs` to a legacy debug surface and removed it from the primary header navigation.
- Updated SIWE default wording to `Trusted Housing Platform` after cleanup verification stayed green.

## Gate 1A 角色認證啟用閉環實作

- 變動部分：`go-service/internal/modules/credential/*`、`go-service/internal/modules/user/identity_contract.go`、`go-service/internal/bootstrap/router.go`、`go-service/internal/bootstrap/wiring.go`
- 變動原因及邏輯：新增 `credential` 模組，接上角色申請、檔案上傳、智能審核、人工審核請求、啟用與 admin review API，並擴充 IdentityNFT 合約 client 支援 `mintCredential` 與角色 token 查詢。這次設計刻意採 `審核通過 != 直接 mint`，而是回到前端顯示「通過待啟用」，由使用者自己決定是否啟用 NFT 憑證。

- 變動部分：`infra/init/08-credential-submissions.sql`、`infra/init/04-onboarding.sql`、`infra/reset_identity.sql`、`go-service/internal/db/model/credential_submission_model.go`、`go-service/internal/db/repository/credential_submission_repo.go`、`go-service/internal/db/repository/user_credential_repo.go`
- 變動原因及邏輯：新增 `credential_submissions` 作為 Gate 1A 的角色申請 write model，保存表單、OCR、智能 / 人工審核結果與啟用狀態；同時把 `user_credentials` 收斂成已啟用角色 read model，補入 `revoked_at` / `revoked_reason`，避免整條申請流程繼續混在單一表上。

- 變動部分：`go-service/internal/modules/credential/event_handler.go`
- 變動原因及邏輯：新增 `CredentialMinted` worker，讓角色 NFT 啟用後可透過 indexer 再次同步回 PostgreSQL，避免身份中心只靠前端暫存或單次 API 回應顯示角色啟用結果。

- 變動部分：`react-service/src/api/credentialApi.ts`、`react-service/src/components/credential/*`、`react-service/src/pages/OwnerCredentialPage.tsx`、`react-service/src/pages/TenantCredentialPage.tsx`、`react-service/src/pages/AgentCredentialPage.tsx`、`react-service/src/pages/IdentityCenterPage.tsx`、`react-service/src/router/index.tsx`
- 變動原因及邏輯：前端正式接上三個角色頁與共享申請殼層，主導文案改成繁體中文，並把身份中心改成 Gate 1A 真實狀態頁。使用者現在可在頁面上選擇「智能審核」為主路徑，也可切換成「人工審核」副路徑；審核結果不代替使用者下決定，只有在使用者按下啟用後才 mint 角色 NFT。

- 變動部分：`docs/開發規劃書.md`、`docs/database/relational-database-spec.md`、`docs/database/relational-database-spec.csv`
- 變動原因及邏輯：把 Gate 1A 從「規劃中」同步為「已接線」，明確寫出 `credential_submissions` / `user_credentials` 的 write-read model 分工，並標示 Gate 1B 權限切換仍未開始，避免後續接手者把角色頁上線誤讀成權限已經切換完成。

- 變動部分：Gate 1A 驗證邊界
- 變動原因及邏輯：本次已完成 `go test ./...`、`go build ./...`、`npm run lint`、`npm run build` 的自動驗證，並修正 reviewer 指出的鏈上啟用 / indexer checkpoint / 身份中心載入 / capability gating 問題。完整人工 smoke checklist 仍需在真實登入、送件、審核與啟用流程上再跑一次，這部分保留為 handoff 驗收項，不在本次 checkpoint 內誤報為已完成。
