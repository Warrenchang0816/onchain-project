# 2026-06-09 dev_log

## 今日主題

導入 `Agent-Project-Constitution` 為 onchain-project（可信房屋媒合平台）的治理基準，並完成首份 Hermes capability audit、建立專案級 `Project_Rules.md`。

## 討論結論

- 使用者明確訂下開發規則：**全套引用 `D:\Git\Agent-Project-Constitution` 作為本專案治理憲法**，與既有 `CLAUDE.md` + 使用者 memory 規則疊加（三者一致，無衝突）。
- 生效原則：語言規則、Source of Truth 順位、Git/Worktree 規則、安全與 secrets、Timeout 強制、驗證流程、dev_log 工作流程、四份檢查清單（before-implementation / before-editing-docs / before-commit / before-claiming-complete）。
- **Hermes-first 審核結果：本專案不適用（休眠）**。理由見下節。
- 依使用者選擇執行 A+B：建立本 dev_log + 建立 `Project_Rules.md`（不 commit，待使用者確認）。

## Hermes capability audit（首份）

- 審核對象：`onchain-project` 現有與可預見範圍。
- 技術棧：Go (Gin + PostgreSQL，無 ORM) REST API、React 19 SPA、Web3 (viem/wagmi、task-reward-vault Hardhat 合約)、Phase 6 eKYC (AWS Rekognition / faceai)。
- 結論：專案**無 LLM-agent runtime、未導入 Hermes、亦非在打造 agent 框架**。Hermes-first 所治理的能力（agent runtime / profile / skill / Kanban / proxy / provider / memory 編排）不在本產品範圍。
- 決議：Hermes-first 標記為 **dormant / N/A**，不作為現階段審核閘門。
- 重新啟用條件：若日後新增 AI-agent 層（自動媒合/協商 agent、多 provider LLM proxy、agent 任務板等），需重跑 Hermes capability audit 並改為 active gate。

## 影響範圍

- 文件：新增 `dev_log/2026-06-09.md`、新增 `Project_Rules.md`（repo 根目錄）。
- 程式碼：無。
- 設定：無。
- 測試：無。

## 未採納方向

- 選項 C（僅口頭遵守、不落文件）：未採納。理由——憲法 `source-of-truth-rules` 要求規則進入正式文件，不可只存在於對話，否則後續 agent 無依據。
- 「全套含 active Hermes-first」：未採納。理由——本專案非 agent 平台，強行套用 Hermes-first 無對象可審核，徒增形式負擔；改為 dormant 並保留重新啟用條件。

## 風險與後續

- 目前位於 `feat/tenant-data-consolidation` 分支；本治理文件與該分支功能無關，屬 untracked 新檔，不會混入 tenant 功能 commit。後續是否獨立分支/PR 提交治理文件，待使用者決定。
- `Project_Rules.md` 落點選 repo 根目錄（與 `CLAUDE.md` 並列、最易被發現）；若團隊偏好放 `docs/`，可再調整。
- `feat/tenant-data-consolidation` 三項功能仍有「待手動驗證」端到端流程未跑（見 2026-06-07 / 2026-06-08 紀錄）。

## 驗證

- 本次僅新增文件，無程式變更，無需 build/test。
- 文件驗證：`git diff --check`（待 commit 前執行）、確認無未完成標記與舊名衝突。
