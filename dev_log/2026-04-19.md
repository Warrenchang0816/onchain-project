# 2026-04-19 開發日誌

規格：變動原因及邏輯 = 時間 + 變動部分

## KYC 流程硬化

- 變動部分：`go-service/internal/modules/onboarding/service.go`
- 變動原因及邏輯：`VerifyEmailOTP` 加入 Email 重複檢查；`RequestPhoneOTP` 加入手機重複檢查；`BindWallet` 加入 person_hash 身分重複檢查（保底）及傳遞 `idNumberHint` 給 `CreateFromOnboarding`。三處均以 sentinel error (`ErrEmailAlreadyUsed` / `ErrPhoneAlreadyUsed` / `ErrIdentityAlreadyUsed`) 回傳衝突者的遮蔽身分字號。

- 變動部分：`go-service/internal/modules/onboarding/handler.go`
- 變動原因及邏輯：對應上方三種 sentinel error，回傳 HTTP 409 + `error_code` + `id_hint`，讓前端能精確呈現衝突提示而非 400 通用錯誤。

- 變動部分：`go-service/internal/db/repository/user_repo.go`
- 變動原因及邏輯：新增 `FindByPhone`；`userSelectCols` 補入 `mailing_address`、`id_number_hint`；`CreateFromOnboarding` 加第七個參數 `idNumberHint`；`scanUser` 同步掃描新欄位。

- 變動部分：`go-service/internal/db/model/user_model.go`
- 變動原因及邏輯：`User` struct 補入 `MailingAddress sql.NullString`、`IDNumberHint sql.NullString`，與 `06-profile-fields.sql` migration 對齊。

- 變動部分：`go-service/internal/db/model/kyc_submission_model.go`
- 變動原因及邏輯：`KYCSubmission` struct 補入 `OCRIDNumber sql.NullString`，配合 `ocr_id_number` 欄位儲存原始身分字號以供 `GetProfile` 使用。

- 變動部分：`go-service/internal/db/repository/kyc_submission_repo.go`
- 變動原因及邏輯：`OCRResultParams` 新增 `IDNumber string`；`SetOCRResult` 將 `ocr_id_number` 存入 DB；SELECT 及 scan 同步補齊。

## 會員資料頁（MemberProfilePage）建立

- 變動部分：`react-service/src/pages/MemberProfilePage.tsx`（新增）
- 變動原因及邏輯：建立會員資料頁，顯示基本資料（身分字號、性別、生日、戶籍地址、通訊地址、Email、手機）、錢包資料、KYC 狀態、角色憑證四大區塊；Email / 手機 / 通訊地址提供 inline OTP 變更流程。

- 變動部分：`react-service/src/api/userApi.ts`
- 變動原因及邏輯：`UserProfile` 型別補入 `idNumber`、`gender`、`birthDate`、`registeredAddress`、`mailingAddress`；新增六個 API 函式（email/phone/mailing address OTP 請求與驗證）。

- 變動部分：`react-service/src/router/index.tsx`
- 變動原因及邏輯：新增 `/profile` 路由對應 `MemberProfilePage`。

- 變動部分：`react-service/src/components/common/Header.tsx`
- 變動原因及邏輯：Header 下拉選單補入「會員資料」連結，路由至 `/profile`。

- 變動部分：`react-service/src/index.css`
- 變動原因及邏輯：補入 profile 頁樣式（`.profile-section`、`.profile-row--editable`、`.profile-edit-panel` 等）及 inline edit 相關 class。

## 使用者模組後端更新（profile change flow）

- 變動部分：`go-service/internal/modules/user/service.go`、`handler.go`
- 變動原因及邏輯：`GetProfile` 補入 OCR 欄位、性別推導、Mint TX 過濾（只顯示 `0x...`）；新增 `RequestEmailChange`、`VerifyEmailChange`、`RequestPhoneChange`、`VerifyPhoneChange`、`RequestMailingAddressOTP`、`UpdateMailingAddress` 六個業務方法及對應 handler。

- 變動部分：`go-service/internal/modules/user/dto.go`
- 變動原因及邏輯：`UserProfileResponse` 補入 `IDNumber`、`Gender`、`BirthDate`、`RegisteredAddress`、`MailingAddress`；新增六個 contact change 相關 request/response DTO。

- 變動部分：`go-service/internal/bootstrap/router.go`
- 變動原因及邏輯：在 `protected` group 下新增六條 profile change 路由（email/phone/mailing-address OTP 請求與驗證）。

- 變動部分：`go-service/internal/bootstrap/wiring.go`
- 變動原因及邏輯：`userSvc` 初始化改接 `otpRepo`、`emailSender`、`smsSender`，使 profile change OTP 能共用 notify 基礎設施。

## Docker 網路修正（go-service 連到 Docker PostgreSQL）

- 變動部分：`infra/docker-compose.yml`、`go-service/docker-compose.yml`、`go-service/.env`
- 變動原因及邏輯：go-service 在 Windows 上透過 `host.docker.internal` 連到本機 native PostgreSQL（port 5432）而非 Docker infra postgres，導致 migration 欄位找不到。建立共用 `onchain` bridge network，go-service 改用 container name `onchain-postgres` / `onchain-minio` 直連。

## KYC 409 前端錯誤呈現

- 變動部分：`react-service/src/api/onboardingApi.ts`
- 變動原因及邏輯：新增 `EmailAlreadyUsedError`、`PhoneAlreadyUsedError`、`IdentityAlreadyUsedError` 三個 sentinel error class；`verifyEmailOTP` 和 `requestPhoneOTP` 解析 409 回應並 throw 對應 class。

- 變動部分：`react-service/src/pages/OnboardingPage.tsx`
- 變動原因及邏輯：import 三個新 error class；新增 `emailConflict`、`phoneConflict`、`identityConflict` 三個 state；各步驟 handler catch 對應 error 後設 state 並顯示衝突提示卡（含身分字號隱碼），不再讓 generic error handler 吞掉。

## MemberProfilePage lint 修正

- 變動部分：`react-service/src/pages/MemberProfilePage.tsx`
- 變動原因及邏輯：`EditButton` 原本定義在 component 內部（render 期間建立 component 違反規則）→ 提取為 module-level function component，props 傳入 `editing`、`onStart`；`useEffect` 內改為直接 fetch（避免 sync setState in effect），`loadProfile()` 保留供 edit 後 refresh 用。

## Task → Listing 全面重構

- 變動部分：`go-service/internal/modules/task/`（移除）、`go-service/internal/modules/listing/`（新增）、`go-service/internal/modules/logs/`（新增）
- 變動原因及邏輯：`task` 模組是舊 compatibility 骨架，與房屋平台語意不符，趁早重構避免技術債累積。`listing` 模組承接所有房源業務邏輯，`logs` 獨立承接 blockchain log 查詢，不再依附 task。

- 變動部分：`go-service/internal/db/model/listing_model.go`（新增）、`go-service/internal/db/model/listing_appointment_model.go`（新增）
- 變動原因及邏輯：新增 listing / listing_appointment 的 Go model struct，對應 DB 欄位，使用 `sql.Null*` 型別處理可選欄位。

- 變動部分：`go-service/internal/db/repository/listing_repo.go`（新增）、`go-service/internal/db/repository/listing_appointment_repo.go`（新增）
- 變動原因及邏輯：listing / listing_appointment 的 raw SQL repository；`ListingFilter.Status = "ALL"` 為 sentinel 繞過狀態過濾（供屋主查自己所有房源）。

- 變動部分：`go-service/internal/db/repository/user_repo.go`
- 變動原因及邏輯：補入 `FindByID(id int64)` 方法，供 listing service 內部查詢用。

- 變動部分：`go-service/internal/modules/listing/dto.go`、`service.go`、`handler.go`（全部新增）
- 變動原因及邏輯：listing module 完整實作。service 以 wallet address 為入口（`FindByWallet`），`requireVerifiedUser` 強制 KYC VERIFIED；handler 解析 gin context 取 wallet；sentinel errors 映射 HTTP 狀態碼。

- 變動部分：`infra/init/07-listings.sql`（新增）
- 變動原因及邏輯：建立 `listings` 與 `listing_appointments` 表；`listings.negotiating_appointment_id` FK 使用 `DEFERRABLE INITIALLY DEFERRED` 解決循環依賴問題。

- 變動部分：`go-service/internal/bootstrap/wiring.go`、`go-service/internal/bootstrap/router.go`
- 變動原因及邏輯：移除所有 task 路由與 wiring，接入 `listing` + `logs` module；wiring 步驟重新編號為 1–14；router 新增 public（可選 auth）與 protected（需 auth）listing routes，含預約系統 API。

- 變動部分：`react-service/src/api/listingApi.ts`（完整重寫）
- 變動原因及邏輯：舊 `listingApi.ts` 是 re-export shim（從 taskApi 轉出），已無法對應新 listing API 介面；完整重寫為 `Listing`、`Appointment`、`ListingStatus`、`ListingType` 型別 + 14 個 API 函式。

- 變動部分：`react-service/src/pages/ListingListPage.tsx`、`react-service/src/pages/ListingDetailPage.tsx`（完整重寫）
- 變動原因及邏輯：`TaskListPage` / `TaskDetailPage` 已與新 listing API 型別不相容，完整重寫；含預約系統 UI、屋主 / 訪客操作區塊、隊列顯示。

- 變動部分：`react-service/src/router/index.tsx`
- 變動原因及邏輯：`/listings` 與 `/listings/:id` 改接 `ListingListPage` / `ListingDetailPage`；移除 `/tasks` 與 `/tasks/:id` 路由。

- 變動部分：`docs/架構設計書.md`、`docs/開發規劃書.md`
- 變動原因及邏輯：第一階段正式宣告完成；更新 package layout、module 職責、DB table 清單、wiring 步驟；第二階段主線定為「身分認證完成（OWNER / TENANT / AGENT 角色申請）」。

## Tailwind + Stitch 設計整合

- 變動部分：`react-service/tailwind.config.cjs`（新增）、`react-service/postcss.config.cjs`（新增）
- 變動原因及邏輯：引入 Tailwind CSS v3；因 `package.json` 設有 `"type": "module"`，設定檔需使用 `.cjs` 副檔名。色票全數對應 Stitch 輸出的 Material Design 3 token（primary-container=#E8B800 / tertiary=#006c4a / surface 系列）。

- 變動部分：`react-service/src/index.css`
- 變動原因及邏輯：在既有樣式前插入 `@tailwind base/components/utilities`；新增 `.material-symbols-outlined` helper class。

- 變動部分：`react-service/index.html`
- 變動原因及邏輯：`lang="zh-TW" class="light"`；`<title>` 改為「去中心化房屋平台」；引入 Inter + Noto Sans TC + Material Symbols Outlined 完整 axis 範圍字型。

- 變動部分：`react-service/src/components/common/Header.tsx`
- 變動原因及邏輯：以 Tailwind 全面重寫；`bg-[#FFFEF9]/80 backdrop-blur-xl sticky top-0`；品牌名稱 + nav active 態皆加 `border-b-2 border-[#E8B800]`；dropdown 改為 rounded-xl 白底浮層。

- 變動部分：`react-service/src/layouts/SiteLayout.tsx`、`react-service/src/layouts/AppLayout.tsx`
- 變動原因及邏輯：以 Tailwind 重寫版面容器與 footer；footer 採暖石色底 `bg-[#f0eee9]`，連結 hover 改綠色。

- 變動部分：`react-service/src/pages/HomePage.tsx`
- 變動原因及邏輯：全面 Tailwind 化；Hero 漸層背景 + 搜尋卡片；Stats 統計三格；平台三層架構 3-col bento；縣市 grid 2/3-col 浮起卡片。

- 變動部分：`react-service/src/pages/ListingListPage.tsx`
- 變動原因及邏輯：Tailwind 重寫；compact hero strip + pill 篩選 tab；同步修正 `listing.type` → `listing.list_type`、`listing.floor_area` → `listing.area_ping` 欄位名稱錯誤。

- 變動部分：`react-service/src/pages/ListingDetailPage.tsx`
- 變動原因及邏輯：Tailwind 重寫；移除對外部 `AppButton`/`AppModal` 的依賴，改用頁內輕量元件；8+4 col layout；TypeScript 編譯通過零錯誤。

## 開發日誌規則修正

- 變動部分：`dev_log/2026-04-18.md`、`dev_log/2026-04-19.md`（新增）
- 變動原因及邏輯：原本三個 2026-04-19 節混在 `2026-04-18.md` 中，違反一天一檔規則；將 18 日內容還原為純 18 日記錄，今日所有內容統一移至 `2026-04-19.md`。

## Stitch 設計二輪整合（ListingListPage / HomePage / ListingDetailPage 完整重寫）

- 變動部分：`react-service/src/pages/ListingListPage.tsx`
- 變動原因及邏輯：完整改寫為 Stitch 第二版設計。Hero 採 `bg-gradient-to-r from-surface to-surface-container-low`，搭配 blur 裝飾球；篩選 tab 改為 `sticky top-[64px] z-40 bg-surface-container-lowest`；卡片升級為 `flex flex-col bg-surface-container-lowest rounded-xl overflow-hidden hover:-translate-y-1`，圖區 `relative h-64 w-full bg-surface-variant`，狀態徽章 `backdrop-blur-md rounded-full`；我的房源 bento 採 `lg:grid-cols-12`（`lg:col-span-8` 特色卡 + `lg:col-span-4` 統計欄）；右下浮動新增按鈕僅限 `md:hidden`。

- 變動部分：`react-service/src/pages/HomePage.tsx`
- 變動原因及邏輯：完整改寫為 Stitch 第二版設計。Hero 採 `lg:grid-cols-2` 兩欄排版，左側 headline + search card + 統計數字，右側裝飾卡片方格；價值主張三欄（KYC / 錢包 / 合約）改為 MD3 token 配色；縣市快速連結改為 `border-l-4 border-primary-container` 左邊框風格；最新房源 `aspect-[4/3]` 圖卡；底部 CTA 採 `bg-gradient-to-br from-primary-container to-primary-fixed-dim`。

- 變動部分：`react-service/src/pages/ListingDetailPage.tsx`
- 變動原因及邏輯：完整改寫為 Stitch 第二版設計。新增 bento 圖片 gallery（`grid-cols-4 grid-rows-2`，主圖 `col-span-3 row-span-2` + 兩個縮圖）；主內容改為 `lg:grid-cols-12`（`lg:col-span-8` 詳情 + `lg:col-span-4` 操作側欄）；所有表單、Modal、ConfirmDialog 的 className 全面改用 MD3 token；保留所有業務邏輯（上架、編輯、預約、確認、鎖定、結案等），TypeScript 零錯誤。

- 變動部分：`react-service/src/layouts/SiteLayout.tsx`
- 變動原因及邏輯：footer 改為 Stitch 輸出樣式（`bg-[#F5F3EE]`、連結 `decoration-[#059669]`）。

## KYC 接續進度步驟精確復原

- 變動部分：`go-service/internal/modules/onboarding/handler.go`
- 變動原因及邏輯：新增 `calcResumeWizardStep(sess)` 函式，依 session 的 `second_doc_path` 與 `selfie_path` 欄位是否有值，推算出精確的前端 wizard step（`second-doc` / `selfie` / `wallet`）。原本只用 backend step `CONFIRMED` 對應 `second-doc`，導致已完成第二證件 + 自拍的用戶接續時仍回到第二證件步驟。Resume response 新增 `ResumeWizardStep` 欄位一併回傳。

- 變動部分：`go-service/internal/modules/onboarding/dto.go`
- 變動原因及邏輯：`VerifyEmailOTPResponse` 新增 `ResumeWizardStep string` 欄位（`json:"resume_wizard_step,omitempty"`）。

- 變動部分：`react-service/src/api/onboardingApi.ts`
- 變動原因及邏輯：`VerifyEmailOTPResponse` 型別新增 `resume_wizard_step?: string`。

- 變動部分：`react-service/src/pages/OnboardingPage.tsx`
- 變動原因及邏輯：resume 判斷改用 `result.resume_wizard_step as WizardStep`（後端精確計算的步驟），fallback 才用 `backendStepToWizardStep(result.step)`，徹底修正 CONFIRMED 永遠對應 second-doc 的問題。

## KYC 流程 UI 改善

- 變動部分：`react-service/src/pages/OnboardingPage.tsx`（綁定錢包步驟）
- 變動原因及邏輯：原「上一步」按鈕改為「重新身份上傳」，點擊後清空所有上傳預覽（`setFiles(initialFiles)` / `setPreviews(initialPreviews)`）、清空所有 OCR 欄位（idNumber / fullName / gender / birthDate / issueDate / issueLocation / address / fatherName / motherName）、並重置資料確認狀態（`confirmEditing=false` / `confirmManuallyEdited=false`），跳回身分證上傳步驟重新來過。

- 變動部分：`react-service/src/pages/OnboardingPage.tsx`（黑底問題修正）
- 變動原因及邏輯：「重新身份上傳」按鈕新增 `bg-transparent`，防止 Edge 瀏覽器 Automatic Dark Mode 自動覆蓋黑色背景。本專案已確立規則：所有 `<button>` / `<div>` 若無明確 `bg-*` 類別，Edge 自動深色模式會套用黑底，**開發時必須對所有元件明確設定背景色**。

## 密碼未設定流程（忘記密碼 / 初次設定密碼）

- 變動部分：`go-service/internal/modules/onboarding/service.go`
- 變動原因及邏輯：`VerifyEmailOTP` 在 email 已存在 users 表的判斷中，新增 `password_hash IS NULL` 子判斷 → 回傳 `ErrEmailNotActivated{Email}` sentinel error（新型別）。原本一律回傳 `ErrEmailAlreadyUsed`，但密碼未設定的用戶應被引導至設定密碼頁面而非被擋下。

- 變動部分：`go-service/internal/modules/onboarding/handler.go`
- 變動原因及邏輯：`VerifyEmailOTP` handler 新增對 `ErrEmailNotActivated` 的捕捉，回傳 HTTP 409 + `error_code: "EMAIL_NOT_ACTIVATED"` + `email`，讓前端能精確導向忘記密碼頁。

- 變動部分：`go-service/internal/modules/auth/reset_password_handler.go`（新增）
- 變動原因及邏輯：新增 `ResetPasswordHandler`，實作兩支公開 API：`POST /api/auth/reset-password/request-otp`（查找 email 對應用戶，60 秒頻率限制，發送 OTP 至信箱）與 `POST /api/auth/reset-password/set-password`（驗證 OTP + bcrypt 設定新密碼），重用 `otp_codes` 表（`session_id=NULL`）。

- 變動部分：`go-service/internal/bootstrap/wiring.go`
- 變動原因及邏輯：在 email sender 初始化後（步驟 10b）建立 `ResetPasswordHandler`，傳入 `userRepo`、`otpRepo`、`emailSender`，並注入 `SetupRouter`。

- 變動部分：`go-service/internal/bootstrap/router.go`
- 變動原因及邏輯：`SetupRouter` 新增 `resetPasswordHandler` 參數；新增兩條公開路由：`POST /api/auth/reset-password/request-otp` 和 `POST /api/auth/reset-password/set-password`。

- 變動部分：`react-service/src/api/forgotPasswordApi.ts`（新增）
- 變動原因及邏輯：提供 `resetPasswordRequestOTP(email)` 與 `resetPasswordSetPassword(email, code, password)` 兩個 API 呼叫函式，對應上方兩支後端 API。

- 變動部分：`react-service/src/pages/ForgotPasswordPage.tsx`（新增）
- 變動原因及邏輯：三步驟頁面：① 輸入 email 發送驗證碼（60 秒重送冷卻）→ ② 輸入 6 位 OTP → ③ 設定新密碼（確認密碼 + 長度驗證）→ 完成後導向登入。URL 支援 `?email=xxx` query string，讓 onboarding 流程可以直接攜帶 email 跳入。

- 變動部分：`react-service/src/api/onboardingApi.ts`
- 變動原因及邏輯：新增 `EmailNotActivatedError` class（攜帶 `email` 欄位）；`verifyEmailOTP` 在 409 回應中優先判斷 `error_code === "EMAIL_NOT_ACTIVATED"` 並 throw，原 `EMAIL_ALREADY_USED` 判斷保持不變。

- 變動部分：`react-service/src/pages/OnboardingPage.tsx`
- 變動原因及邏輯：import `EmailNotActivatedError` 與 `logout`（來自 authApi）；catch `EmailNotActivatedError` 後呼叫 `navigate("/forgot-password?email=...")` 直接引導；新增 `useRef` 追蹤 `step` 和 `passwordSaved` 最新值，unmount cleanup 時若 `step==="done"` 且 `!passwordSaved` 自動呼叫 `logout()`，確保密碼尚未設定的用戶離開頁面後不保持登入狀態。

- 變動部分：`react-service/src/pages/LoginPage.tsx`
- 變動原因及邏輯：「忘記密碼？」從 `<a href="#">` 改為 `<Link to="/forgot-password">`，真正連接忘記密碼頁面。

- 變動部分：`react-service/src/router/index.tsx`
- 變動原因及邏輯：新增 `/forgot-password` 路由對應 `ForgotPasswordPage`。
