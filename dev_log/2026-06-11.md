# 2026-06-11 dev_log

## 今日主題

緊急事件：本機 Docker 全部 image / container / volume 遭誤刪清空。依專案文件與程式架構，從零把建立在 Docker 上的 image 補回，並由獨立驗證者 agent 實證確認專案可正常運行。順手釘死浮動版本 tag，避免重蹈覆轍。

## 討論結論

- 掃描確認本專案實際跑在 Docker 上的 image 只有 4 個：`postgres:16-alpine`、`redis:7-alpine`、`minio/minio`、本地 build 的 `go-service:1.0.0`。`react-service`（npm）、`contract-service`（Hardhat）不在 Docker 上。
- 所有 image「定義」都在 git（compose + Dockerfile），被刪的只是本機快取與 volume → 復原為機械式「重新 pull + 重新 build」。
- 啟動順序有相依：先 `infra`（建 `onchain` 外部網路 + postgres/redis/minio），再 `go-service`。
- 採「開發者執行 + 獨立驗證者 agent 確認」分工。
- 版本釘選策略：先用現有 tag 重建並驗證成功，再把**實際 pull 到的版本**回寫釘死（釘的是確認可運作的版本，不猜 tag）。

## 影響範圍

- 設定/基礎設施（已套用並重建）：
  - `infra/docker-compose.yml` — `minio/minio:latest` → `minio/minio:RELEASE.2025-09-07T16-13-09Z`（已重新 pull + recreate minio 容器驗證 tag 可拉取）。
  - `go-service/Dockerfile` — `debian:stable-slim` → `debian:12-slim`（與現行 stable 內容相同，下次 build 生效）。
- `postgres:16-alpine` / `redis:7-alpine` 已是不跳大版本的 tag，維持不動。
- 程式碼：無。
- 資料：postgres/redis/minio 三個 volume 全新重建；postgres 重跑 `infra/init/*.sql`，`kyc` bucket 由 go-service 啟動時 `EnsureSchema` 後自動建立。

## 未採納方向

- postgres/redis 釘到 exact patch tag：未採。已是 `16-alpine`/`7-alpine`，不會跳大版本；釘 patch 反增「該 patch-alpine tag 不存在則 pull 失敗」風險。
- 用 image digest（`@sha256:`）釘選：未採。RELEASE tag 對 MinIO 更可讀且同樣精確。
- 現場順手修掉 demo seed bug：未採（與本次救火無關、會動到 goal 主線資料層）——改為記錄、待獨立處理。

## 風險與後續（既有 bug，非本次造成）

- **`infra/init/15-demo-properties.sql` 執行順序 bug**：它在 postgres-init 階段就 `INSERT INTO property / rental_listing / sale_listing`，但這三張表的真正建表來源是 go-service 啟動時的 `EnsureSchema`（`go-service/internal/platform/db/schema.go`），init 階段時尚未存在 → seed 每次全新 volume 都報錯中斷（`relation "rental_listing" does not exist`），導致這三表 demo 永遠 0 筆。
- 影響：純 demo 資料缺失，不影響專案運行（表存在、功能正常）。任何人從乾淨 volume 全新啟動都會遇到。
- 建議修法：把該 demo seed 移到 go 端、`EnsureSchema` 之後執行（或 go-service 起來後手動跑），**不要**在 `infra/init` 補 CREATE TABLE（會與 EnsureSchema 重複定義）。
- 記憶已存：`memory/project_schema_source_of_truth.md`。

## 驗證（獨立驗證者 agent 實證，全 PASS）

- 容器：`docker ps` → 4 容器 Up，postgres/minio healthy。
- API：`GET http://localhost:8081/api/locations/districts` → HTTP 200 + 正確 envelope。
- Schema：`docker exec onchain-postgres psql -U postgres -d LAND -c "\dt"` → 29 張表；`taiwan_districts` 368 筆、`listings` 5 筆。
- 新 schema 表存在性（控制端親查）：`property` / `rental_listing` / `sale_listing` / `customer` 皆存在（各 0 筆，因上述 seed bug）；`properties` 已被 rename 成 `customer`。
- MinIO：`kyc` bucket 由 go-service 自動建立成功。
- go-service log：無 panic/fatal，結尾 `Listening and serving HTTP on :8081`。
