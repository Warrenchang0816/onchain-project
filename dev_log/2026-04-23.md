# 2026-04-23 開發日誌

規格：變動原因及邏輯 = 時間 + 變動部分

## Gate 1A Credential UX Refinement

- 變動部分：`go-service/internal/modules/credential/domain.go`、`domain_test.go`、`dto.go`、`service.go`
- 變動原因及邏輯：新增 `STOPPED` 狀態常數與 `CanStopReview`、`DisplayStatusForSubmission` 純函式，讓前端能透過一致的顯示狀態映射驅動 UI，不再依賴 ad-hoc 邏輯。

- 變動部分：`go-service/internal/db/repository/credential_submission_repo.go`
- 變動原因及邏輯：新增 `MarkStopped` 方法，將指定 submission 的 `review_status` 設為 `STOPPED`。

- 變動部分：`go-service/internal/modules/credential/handler.go`、`service.go`、`bootstrap/router.go`
- 變動原因及邏輯：新增四支 API（最新 submission 詳情、停止審核、主/補充文件下載），讓前端能在唯讀成品頁直接查看文件與執行操作。

- 變動部分：`react-service/src/api/credentialApi.ts`
- 變動原因及邏輯：新增 `CredentialSubmissionDetail` 型別與三個對應函式，讓前端 API 層與後端新端點對齊。

- 變動部分：`react-service/src/components/credential/CredentialDocumentUploader.tsx`
- 變動原因及邏輯：選擇檔案後立即透過 `URL.createObjectURL` 產生預覽圖，並在 effect cleanup 正確撤銷，避免 memory leak。

- 變動部分：`react-service/src/components/credential/CredentialConfirmDialog.tsx`（新增）
- 變動原因及邏輯：建立共用確認彈窗，供智能審核、人工審核、停止審核、啟用身份四個關鍵操作使用，確保使用者有明確的確認步驟。

- 變動部分：`react-service/src/components/credential/CredentialSubmissionSnapshot.tsx`（新增）
- 變動原因及邏輯：建立唯讀成品頁元件，送件後切換至此頁面展示填寫內容與文件預覽，不再讓使用者停留在可編輯表單。

- 變動部分：`react-service/src/components/credential/CredentialApplicationShell.tsx`
- 變動原因及邏輯：重構為雙模式（編輯 / 唯讀成品），加入確認彈窗、停止審核、重新審核、啟用身份等操作，並把人工審核改為表單下方的次要 CTA 文字。

- 變動部分：`react-service/src/components/credential/CredentialRolePage.tsx`（新增）
- 變動原因及邏輯：抽出共用的角色頁容器，統一處理 auth check、parallel fetch、refresh callback，讓三個角色頁可精簡為薄包裝。

- 變動部分：`react-service/src/pages/OwnerCredentialPage.tsx`、`TenantCredentialPage.tsx`、`AgentCredentialPage.tsx`
- 變動原因及邏輯：改為 `CredentialRolePage` 的薄包裝，每頁只定義角色專屬欄位與文案。

- 變動部分：`react-service/src/pages/IdentityCenterPage.tsx`
- 變動原因及邏輯：新增 `STOPPED` 狀態的顯示處理，引導使用者重新審核。

## 驗證結果

- `go test ./...`：PASS
- `go build ./...`：PASS
- `npm run lint`：PASS（0 errors）
- `npm run build`：PASS
