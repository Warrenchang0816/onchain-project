# 2026-05-07 開發紀錄

## 出租媒合搜尋基礎 (PR #37)

- 後端新增租客需求搜尋 API，支援區域、價格、格局等過濾條件。
- 前端實作 `RentalRequirementsPage`：租客需求管理頁面。
- 前端新增需求 vs 物件媒合基礎流程。

## 物件刊登頁面重設計 (PR #40 feat/property-listing-frontend)

- 前端 `RentListPage` / `SaleListPage` 重設計：統一使用 `ListingSearchFilters` 共用元件。
- 新增 `ListingSearchFilters`：通用搜尋 / 篩選元件，支援建物類型、格局、價格範圍。
- `RequirementsPage` 遷移至 `ListingSearchFilters` 架構。
- 修正 `RentListPage` 篩選器 useEffect 同步問題。
- 修正 `rental_listing` 後端 status filter 邏輯、GenderRestriction 型別、building_type 標籤。

## 搜尋篩選 + 身份中心導航 (PR #41 feat/listing-search-identity-nav)

- `SaleListPage` 新增搜尋篩選（建物類型、格局、總價範圍）。
- 屋主管理頁、租客管理頁新增「← 返回身份中心」連結。
- Demo 種子資料擴充：補齊 20 筆物件的 `auxiliary_area`、`balcony_area`、`building_age`、`management_fee` 等欄位。

## 驗證紀錄

- PASS：TypeScript tsc --noEmit
- PASS：npm run lint
- PASS：Go backend build
